## VLAN

虚拟局域网(Virtual Local Area Network)，是将一个物理的LAN在逻辑上划分成多个广播域的通信技术。

主要作用：隔离广播域。例如：VLAN 10（办公），VLAN 20（监控）。

一个VLAN一般为一个逻辑子网。VLAN内的设备间可以直接通信，不同VLAN间不能直接互通，需要通过三层设备实现相互通信。

广播报文所能到达的整个访问范围成为“二层广播域”，简称广播域，同一广播域内的主机都能接收报文。

局域网内部，一般使用交换机：交换机的吞吐量更大。

交换机：打标记，802.1q。vlan有12bit，总数量1-4094，默认vlan-1。规划不同的vlan标记即可隔离广播域。

### 以太网二层接口类型

* Access口常用来连接用户PC，服务器等终端设备的接口。只能加入一个VLAN。

* Trunk接口允许多个VLAN的数据帧通过。trunk接口常用于交换机之间的互联。

### 广播域

广播能够发送的范围，无论中间隔了多少交换机。

广播报文所能到达网络的整个访问范围称为二层广播域，简称广播域，同一广播域内的主机都能收到广播报文。广播报文一般采用ARP协议进行广播。一个 MAC 地址全为 1 的地址（ FF-FF-FF-FF-FF-FF ）就是广播地址，所有节点都会把目的地址处理为广播地址的数据帧，这个数据帧能到达的整个访问范围就称为二层广播域，也就是广播域。正常来说，通过广播地址就可以访问到同一个网络中所有的设备，广播域的目的就是要让同一个网络中的所有设备都能收到报文。二层交换机需要把广播帧从除源端口之外的端口转发出去，所以二层交换机不能够隔离广播。
            
##  资料来源

[1] https://blog.csdn.net/qq_46286412/article/details/134010738
