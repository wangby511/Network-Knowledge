## Pathlet Routing

Pathlet Routing 是一种把网络路径拆成可组合片段，由发送方选择并拼接这些片段完成端到端路由的机制。

Pathlet Routing 解决的是：

* 可组合性 - 小片段可以拼出很多路径

* 可扩展性 - 比全路径显式存储更省状态

* 策略表达能力强 - 可以表达：经过某个AS，避开某个区域，经过特定安全网关，经过特定带宽链路。

Pathlet Routing

网络运营者：把网络划分成多个“路径片段”，给每个片段一个ID（FID），在路由器里安装转发表。

发送方：在数据包里携带一个FID 序列，明确指定要经过哪些pathlet。

```
传统：   A ----> D  （路由协议决定）

Pathlet：A -> [101] -> B -> [202] -> D
```
